name: Sync PR to Port

on:
  pull_request:
    types: [opened, reopened, synchronize]

jobs:
  sync-to-port:
    runs-on: ubuntu-latest
    steps:
      - name: Get Port token
        id: token
        run: |
          TOKEN=$(curl -s -X POST https://api.getport.io/v1/auth/access_token \
            -H "Content-Type: application/json" \
            -d "{\"clientId\":\"${{ secrets.PORT_CLIENT_ID }}\",\"clientSecret\":\"${{ secrets.PORT_CLIENT_SECRET }}\"}" \
            | jq -r '.accessToken')
          echo "token=$TOKEN" >> $GITHUB_OUTPUT

      - name: Upsert PR entity in Port
        run: |
          curl -s -X POST \
            "https://api.getport.io/v1/blueprints/githubPullRequest/entities?upsert=true&merge=true" \
            -H "Authorization: Bearer ${{ steps.token.outputs.token }}" \
            -H "Content-Type: application/json" \
            -d "{
              \"identifier\": \"${{ github.event.pull_request.id }}\",
              \"title\": \"${{ github.event.pull_request.title }}\",
              \"properties\": {
                \"status\": \"open\",
                \"link\": \"${{ github.event.pull_request.html_url }}\",
                \"prNumber\": ${{ github.event.pull_request.number }},
                \"branch\": \"${{ github.head_ref }}\",
                \"createdAt\": \"${{ github.event.pull_request.created_at }}\",
                \"updatedAt\": \"${{ github.event.pull_request.updated_at }}\"
              },
              \"relations\": {
                \"repository\": \"${{ github.event.repository.name }}\",
                \"service\": \"shipment\"
              }
            }"

      - name: Trigger Port AI PR Review
        run: |
          curl -s -X POST \
            "https://api.getport.io/v1/agent/pr-review-summarizer/invoke" \
            -H "Authorization: Bearer ${{ steps.token.outputs.token }}" \
            -H "Content-Type: application/json" \
            -d "{
              \"prompt\": \"Analyze this pull request and generate a review summary:\n\nPR ID: ${{ github.event.pull_request.id }}\nPR Title: ${{ github.event.pull_request.title }}\nBranch: ${{ github.head_ref }}\nStatus: open\nLink: ${{ github.event.pull_request.html_url }}\nLinked Service: shipment\nRepository: ${{ github.event.repository.name }}\n\nPlease fetch the full entity details, check the service scorecard and incidents, then write a complete AI review summary and risk level back to this PR entity.\"
            }"
